const isHighpriestActive = function() {
  let highpriestActive = false;
  // let users = Object.keys(window.controller.users);
  
  // for (let i = 0; i < users.length; i++) {
  //   if (window.controller.users[users[i]].class === 'highpriest') {
  //     highpriestActive = true;
  //   }
  // }

  return highpriestActive;
};

export default isHighpriestActive;